#ifndef ROUTE_CONFIG_H
#define ROUTE_CONFIG_H

#include <Arduino.h>

// ─── Node đơn giản: chỉ có 1 node (Node 1) ────
// Robot đi thẳng theo line, thấy đỏ → dừng → mở Node 1
#define FIRST_NODE_ID   1

#endif
