# Changelog

## [Unreleased]

### Changed
- **Tự dò vị trí màu sắc + gesture — robot demo-ready không cần chỉnh cổng (17/08)**: bỏ cố định MUX ch2/ch1 cho color sensor (TCS34725 `0x29`) và gesture (PAJ7620 `0x73`). `SensorManager::begin()` giờ tự quét: A3/Wire trực tiếp trước, rồi MUX `ch2`/`ch1` cấu hình cũ, rồi toàn bộ `ch0..7` — thiết bị cắm bất kỳ đâu cũng được tìm thấy, không cần sửa code/sửa dây. Line tracer giữ cố định A3/I2C0 (đã chứng minh qua heartbeat `raw=NO_RESPONSE`). In vị trí tìm thấy từng sensor ra Serial (log `@ A3/Wire` / `@ MUX chX`) giúp xác định dây thật 30 giây. Build sạch PlatformIO (RAM 47.8%, Flash 48.8%).

### Docs
- **`DEMO-GUIDE.md` — cẩm nang 1 trang chạy demo tour (17/08)**: thay thế cách "test từng cảm biến" bằng kịch bản demo 4 phút đúng 7 bước (đặt đầu tuyến → START từ app → robot bám line/đi tiếp theo nút → dừng điểm đỏ mở node → vẫy tay gesture → PIR dừng cảnh báo → ALL_DONE + huy chương); mục trước demo 10 phút (bố trí line + 5 điểm đỏ, nạp firmware, warm-up PIR ≥1 phút); mục xử lý trục trặc 30 giây (line không bám → check Port A3 + biến trở, không dừng đỏ → điểm đỏ đúng trên line, app không kết nối → quên thiết bị BLE, PIR báo giả → dây hở / `PIR_MODE:HIGH`); kịch bản 3 bước sau demo. Nhấn mạnh thay đổi firmware: không cần phần cứng đúng cổng cho demo.
- **`TEST-ROBOT.md` — chương trình kiểm thử robot + tích hợp app cho buổi chốt dự án (17/08)**: tài liệu test phần cứng firmware (PlatformIO) + tích hợp BLE với app trước ngày chốt: mục 0 chốt thông số hardware đã sửa (chỉ M1/M2, chiều motor đã đảo, line tracer cổng A3/I2C0, PIR_MODE, BLE không cần `\n`); checklist khởi động + SCAN I2C + heartbeat; test motor/LED (MOTOR_TEST 1/3); test line tracer (trắng/đen/ngã ba/đỏ); chạy tuyến 5A/5B (bám line, rẽ, dừng đỏ, PIR cắt ngang, mất line, SOS); PIR & công tắc (warmup 60s, PIR_MODE:LOW/HIGH, SWITCH_PRESS, SOS 10s); gesture; tích hợp app↔robot (auto-connect ≤15s, NODE_START/COMPLETE, SOS 2 chiều, ALL_DONE); GATE A0–GATE E chốt dự án (3 vòng tuyến liên tục, metric đo 5 lần, tag v1.0.0 sau khi pass).

### Fixed
- **tsc sửa lỗi duy nhất còn lại `node/[id].tsx:168` (17/08)**: `pickViEn(node.description, node.descriptionEn)` — `description?` là optional (string|undefined) không gán được vào tham số `string` → thêm `?? ""`. `npx tsc --noEmit` giờ sạch hoàn toàn 0 lỗi; cùng lúc `npx expo lint` 0 lỗi (3 warning có sẵn) và `npx jest` 11/11 pass — đạt GATE 4/GATE 5 tĩnh của TEST-ROBOT.

### Fixed
- **Line tracer chuyển sang đọc trực tiếp cổng I2C0 (Port A3) — kiểu B3 (16/08)**: `raw=NO_RESPONSE` từ heartbeat chứng minh line tracer KHÔNG có trên bus Wire1/MUX. SensorManager giờ trỏ `_lineTracer` vào `MiniR4.I2C0.MXLineTracer` (Wire trực tiếp, đúng như code B3 hoạt động). Đồng thời: lệnh `PIR_MODE:LOW`/`HIGH` đảo mức báo PIR (module active-low: người → chân xuống LOW, đổi sang INPUT_PULLUP + đảo logic; tự kiểm chứng qua `pir=`+`pm=` trên heartbeat), quét I2C toàn bộ cổng (`SCAN A3/...`, `MUX0x70`, `ch0..7`) báo qua BLE mỗi lần kết nối + Serial lúc khởi động. Build sạch PlatformIO.
- **Heartbeat + DUMP kèm giá trị thô 10 kênh line tracer `raw=s1..s10` (16/08)**: heartbeat cho thấy `err=0.2 w=0` — không rõ sensor đang đọc được gì (sai cổng MUX vs I2C0) hay đọc được nhưng ngưỡng/montage sai. Thêm `SensorManager::readLineRaw()` (gọi `getAllSensors`) in 10 giá trị thô vào heartbeat 2s và DUMP 400ms — nếu `raw=NO_RESPONSE` → sensor không ở bus đang đọc (chuyển sang Wire trực tiếp kiểu B3 `MiniR4.I2C0`); nếu có giá trị → so sánh nền trắng vs vạch đen để chỉnh `LINE_THRESHOLD`. Build sạch PlatformIO.
- **Heartbeat kèm giá trị cảm biến (16/08)**: user chỉ thấy `STATUS:heartbeat` mà không thấy dòng DUMP (nghi chưa flash đúng bản hoặc 8s trôi qua). Giờ mỗi heartbeat 2s mang sẵn `err/w` (line), `pir` HIGH/LOW, `color` — chỉ cần nhìn dòng heartbeat là biết sensor đọc được gì, không cần lệnh. Build sạch PlatformIO.
- **`SENSOR_DUMP` tự chạy 8s ngay khi kết nối BLE + alias `DUMP` (16/08)**: user không thấy log DUMP khi gửi lệnh → nghi lệnh không khớp/không tới. Giờ chỉ cần kết nối là robot tự bắt đầu dump cảm biến (không cần gửi lệnh), lệnh chấp nhận thêm `DUMP`. Build sạch PlatformIO.
- **Thêm lệnh chẩn đoán `SENSOR_DUMP` (16/08)**: robot giờ chạy nhưng **chạy thẳng không bám line** (err=0 mọi lúc → drive thẳng vô định) và **PIR không báo**. Code tham chiếu B3 đọc line tracer trực tiếp `MiniR4.I2C0.MXLineTracer` (cổng I2C0) còn code hiện tại đọc qua MUX/Wire1 — nghi cắm sai cổng. `SENSOR_DUMP` in mọi cảm biến mỗi 400ms trong 8s qua BLE+Serial: `err/w/junc` (line), `color`, `gest`, `pir` (HIGH/LOW), `v` (pin) — biết ngay sensor nào chết/không đọc được. Build sạch PlatformIO.
- **Đảo chiều động cơ M1/M2 — robot đi LÙI → đi TỚI (16/08)**: team xác nhận robot chỉ có 2 bánh kéo ở M1/M2 (không có M3/M4) và MOTOR_TEST:40:40 làm robot chạy lùi → `setup()` đảo `M1.setReverse(false→true)`, `M2.setReverse(true→false)` để `+power` = đi tới; `DriveDC.begin(1,2,false,true→true,false)` đồng bộ cùng pha. Rẽ trái/phải đảo theo nên chiều xoay thực tế vẫn đúng. Build sạch PlatformIO.
- **`MOTOR_TEST1` / `MOTOR_TEST3` — test riêng từng cặp cổng để tìm cặp dây thật (16/08)**: sau khi BLE thông, MOTOR_TEST:40:40 làm robot **chạy nhưng đi LÙI** (cả 2 bánh quay). Không thể biết cặp nào (M1/M2 hay M3/M4) đang kéo robot từ lệnh drive đồng thời → tách helper `startMotorTest(pair, args)`: `MOTOR_TEST1:<L>:<R>` chỉ drive M1/M2 (setPower), `MOTOR_TEST3:<L>:<R>` chỉ drive M3/M4 (setSpeed, M4 nghịch dấu), `MOTOR_TEST:<L>:<R>` vẫn drive cả 2. Echo tương ứng `STATUS:motor_test1:ok`/`motor_test3:ok`. Build sạch PlatformIO.
- **BLE khoan dung lệnh thiếu ký tự kết thúc + heartbeat chẩn đoán (16/08)**: MOTOR_TEST không có echo và không bánh nào quay → nghi lệnh chưa bao giờ tới đủ (nRF Connect quên bật end-of-line → `\n` không đến). `ble_handler.cpp` giờ tự coi lệnh là hoàn tất nếu 150ms không nhận thêm byte (`_lastWriteMs`), không cần `\n`/`\r` nữa. Thêm `STATUS:heartbeat` gửi mỗi 2s khi đã kết nối để kiểm tra ngay chiều robot→điện thoại (Notify) trước khi đổ lỗi phần cứng. Build sạch PlatformIO.
- **`MOTOR_TEST` mở rộng chạy CẢ 2 cặp cổng M1/M2 + M3/M4 để xác định dây thật (16/08)**: MOTOR_TEST:40:40 không làm bánh quay ở cặp M1/M2 → nghi ngờ dây động cơ không nằm ở cặp đang điều khiển. Giờ lệnh drive đồng thời M1/M2 (`setPower`) VÀ M3/M4 (`setSpeed`, quy ước team: M4 nghịch dấu) trong 2 giây — cặp cổng nào có bánh quay chính là cặp robot đấu dây. Thêm BLE echo `STATUS:motor_test:ok`/`bad_format` (biết chắc robot có nhận lệnh — nếu không thấy echo → lỗi gửi `\n` qua nRF Connect) + log pin `batt=X.XXV` (loại trừ nguồn yếu → PWM không đủ khởi động tải). Thêm `stopAllMotors()` dừng cả 4 cổng hết cửa sổ test (motors.stop() chỉ chạm M1/M2). Build sạch PlatformIO.
- **Robot nhận START nhưng bánh xe vẫn không quay — thêm lệnh chẩn đoán `MOTOR_TEST:<L>:<R>` (16/08)**: sau khi revert đúng cổng M1/M2 (mục dưới), thêm lệnh BLE `MOTOR_TEST:40:40` → 2 bánh quay đúng 2 giây rồi dừng (xử lý trong `handleIdle()` qua `motorTestUntil`) để team phân biệt ngay: (a) bánh quay khi test nhưng không chạy trên tuyến → lỗi logic leg/STEP_MIN_MS; (b) bánh không quay kể cả test → lỗi phần cứng (wiring/nguồn/`setPower`), kê robot lên trước khi gửi. Build sạch PlatformIO.
- **LED theo trạng thái (16/08, theo yêu cầu team)**: quy ước 3 màu rõ ràng — **dừng (IDLE / AT_NODE / WAIT_CLEAR / END / mất BLE) → xanh dương `(0,0,255)`**, **di chuyển (FOLLOW_LINE, gồm cả "đi tiếp") → xanh lá `(0,255,0)`**, **SOS (nút app / công tắc giữ ≥10s) → đỏ `(255,0,0)`**. Thêm helper `setLedStopped/setLedMoving/setLedSos` (`main.cpp`); sửa các chỗ còn sai: connect/disconnect từ đỏ/xanh lá → xanh dương, WAIT_CLEAR từ vàng cam → xanh dương, FAILED từ đỏ → xanh dương, START qua BLE THIẾU set màu xanh lá (giờ có), nhánh "đi tiếp" (NODE_DONE/NEXT_NODE/VOICE_NEXT) chưa set màu xanh lá (giờ có), AT_NODE/END → xanh dương.
- **Node Entrance trên bản đồ app đổi thành "Kết thúc" và nằm dưới node 4 (16/08)**: `heritage-buddy-app/src/data/museum-map.ts` — `title: "Cổng vào bảo tàng"` → `"Kết thúc"` (`titleEn: "Finish"`), `y: 2` → `y: 40` (nằm DƯỚI node 4 y=33, đúng vị trí node cuối tour); giữ `order: 5` (vẽ sau node 4 trong MapPath). Mô tả giữ ý "đã quay về cổng bảo tàng, hành trình hoàn thành".

### Fixed
- **Robot nhận START nhưng KHÔNG di chuyển — điều tra & revert đúng cổng (16/08)**: khi điều tra ban đầu, code tham khảo `WRO 2026 B3` của team dùng `M3.setSpeed/M4.setSpeed` nên firmware tạm chuyển sang M3/M4 (commit `62ef368`). **Verify lại với team: robot HERITECH đấu dây động cơ kéo ở M1 (trái) / M2 (phải)** — đã revert toàn bộ `src/` về đúng wiring M1/M2 (`MiniR4.M1/M2.setPower()` + `setup()` giữ `setPPR_RPM(545,200)`, `setReverse(false/true)`, `DriveDC.begin(1,2,false,true)`), giữ lại guard `STEP_MIN_MS` (500ms — chống "robot đứng ngay tại ngã ba/đỏ lúc bắt đầu đọc đúng type TỨC THÌ → nhảy bước liên tiếp → nhìn như không chạy") và debug log 2s mỗi bước từ `f5fd836`. Build sạch PlatformIO (RAM 47.9%, Flash ~47.0%). Test thực tế: `pio run -t upload` → bấm START, mở Serial Monitor xem `[LEG] leg/step/junc/err/w` — nếu motor quay ngược chiều đổi `setReverse()`; nếu step nhảy nhanh không rời vạch đỏ → cờ `STEP_MIN_MS` đang hoạt động như thiết kế.

### Added
- **Entrance = node cuối, chỉ tính khi quay về (16/08, theo yêu cầu team)**: 
  - `heritage-buddy-app/src/data/museum-map.ts` — node `entrance` chuyển từ order 0 → **order 5** (hiện SAU node 4 trên bản đồ); lúc xuất phát KHÔNG tính (badge tối đa 4/5 đến khi về); mô tả đổi thành "đã quay về cổng bảo tàng, hành trình hoàn thành".
  - `heritech_robot/src/main.cpp` — khi LEG5 về tới Finish (= Entrance vật lý): thay vì `ALL_DONE` ngay, giờ gửi `NODE_START:0` (app mở màn hình Entrance) + `NODE_COMPLETE:0` (đánh dấu Entrance ✓ → badge 5/5 xanh) rồi vào `AT_NODE`; tín hiệu "đi tiếp" (`NODE_DONE`/`NEXT_NODE`/`VOICE_NEXT`) tại Finish → `ALL_DONE` + END (màn hình Celebration).
  - `heritage-buddy-app/src/app/node/[id].tsx` — node cuối = order lớn nhất (trước đây hardcode `order === 13` — không bao giờ đúng → nút "kết thúc tour" chết); Entrance không có video → hiện mascot Buddy + mô tả (player null không tải gì), ẩn nút "Hỏi Buddy" (nội dung trống); nút "Đi tiếp" thành "Kết thúc tour" dù Entrance đã đánh dấu ✓ (nút vẫn màu xanh ngọc lục bảo).

- **Test gesture bất kỳ lúc nào (16/08 — tạm thời để test cảm biến)**: `heritech_robot/src/main.cpp` `checkGesture()` bỏ gate "chỉ khi AT_NODE" — robot giờ gửi `GESTURE:SWIPE_RIGHT/LEFT` ngay khi nhận cử chỉ, kể cả đang chạy; giữ log `[GESTURE] raw=` để debug cảm biến. App vẫn tự chặn khi không ở màn hình node (không còn cần chờ AT_NODE để xác nhận cảm biến có hoạt động).
- **App rút từ 13 node xuống 5 node (chốt mục B câu 1 — khớp tuyến thật 4 điểm dừng)**: `heritage-buddy-app/src/data/museum-map.ts` — `MUSEUM_NODES` giờ gồm **Entrance (order 0)** + 4 điểm dừng (ancient-01/02/03 + medieval-01 — tạm lấy 4 vật đầu theo yêu cầu team) với `order 1..4` khớp đúng NodeID 1..4 firmware gửi trong `NODE_START:<idx>`; `NODE_START:0` không bao giờ được gửi nên Entrance không mở nhầm. Giữ nguyên `MUSEUM_SECTIONS` (4 section, hiện không được render). Lưu ý còn: badge tiến độ hiển thị tối đa 4/5 (Entrance không tính hoàn thành).

- **Điều hướng leg-based (mục B — theo `PLAN-MOVEMENT-FINAL.md`)**: thay "bám line liên tục + đỏ=node (13 node)" bằng **5 chặng thao tác rời rạc** dừng hẳn mỗi node, chỉ rời khi app gửi "đi tiếp".
  - `heritech_robot/src/route_config.h` (MỚI): bảng `ROUTE_NODES` 5 node (Entrance → History → Ceramics → Artifacts → Special → Finish) + `NodeManager` (chỉ quản lý index, `arrivedAtNext()`); **xóa `node_manager.*` cũ** (13 node, `TOTAL_NODES` — xung đột tên class `NodeManager`).
  - `heritech_robot/src/maneuver_nav.h` (MỚI): `LegExecutor` + 5 legs (`LEG1..5_STEPS` theo tuyến thật: chuỗi ngã ba trái/phải đã xác nhận), non-blocking, `M_FWD_TO_JUNCTION`/`M_BACK_TO_JUNCTION`/`M_TURN_*`/`M_FWD_TO_RED`; thêm `LegResult::FAILED` + `TURN_TIMEOUT_MS` — rẽ quá lâu không thấy line → dừng + về IDLE thay vì xoay vô hạn; `junctionMatches()` dùng chung `JUNCTION_CONFIRM_FRAMES`.
  - `heritech_robot/src/motor_control.h/.cpp`: THÊM `turnLeft90()/turnRight90()` (xoay tại chỗ `TURN_SPEED=35`, KHÔNG sửa PID) + `isLineCentered()` (thoát pha rẽ khi |err|<0.8 && w∈[2,4]); `config.h` thêm `TURN_SPEED/LINE_CENTER_*/TURN_TIMEOUT_MS`, bỏ `TOTAL_NODES`.
  - `heritech_robot/src/main.cpp`: START (nút/app) → `legExec.start(1)`; `handleFollowLine()` chạy `legExec.update()` + result (DONE → AT_NODE / Finish → `ALL_DONE`+END; FAILED → IDLE); `handleAtNode()` gửi `NODE_START:<nodeId>` (1..4); **3 lệnh `NODE_DONE:<id>`/`NEXT_NODE`/`VOICE_NEXT` gộp 1 nhánh** — chỉ có hiệu lực khi `AT_NODE`, bắt đầu leg kế + vẫn gửi `NODE_COMPLETE:<completed>` cho map ✓; `RESUME` tiếp tục leg dở (nếu chưa từng START → chạy leg 1); **giữ nguyên** `checkJunction()`/WARN:turn song song, PID, WAIT_CLEAR tự-resume, SOS, mất-BLE auto-stop. Build sạch PlatformIO (RAM 47.9%, Flash 47.1%).

- **`PLAN-MOVEMENT-FINAL.md` (mới, thư mục gốc)**: plan di chuyển CUỐI CÙNG — chuyển từ "bám line liên tục + đỏ=node" sang điều hướng theo 5 chặng thao tác rời rạc (leg-based: `route_config.h` NodeManager 5 node + `maneuver_nav.h` LegExecutor, 2 file team cung cấp chưa có trên máy). Nội dung: bảng tuyến thật 5 chặng + chuỗi ngã ba từng leg, thay đổi từng file trong `heritech_robot` (thêm `turnLeft90/turnRight90/isLineCentered` vào MotorControl, tích hợp 4 điểm trong main.cpp, xóa `node_manager.*` cũ 13 node — xung đột tên NodeManager), danh sách GIỮ NGUYÊN (PID, checkJunction WARN:turn song song, WAIT_CLEAR/SOS/mất BLE), 4 điều kiện mở cần chốt trước khi code (app 13→5 node, bỏ NODE_COMPLETE, rẽ line-centered vs IMU `turnByAngle`, vị trí sensor khi lùi), lộ trình test 3 vòng (bàn → tuyến thật từng leg → chỉ tiêu đo) và kế hoạch 16–17/08 với fallback về mode cũ (đã build sạch `4ce2bd4`) để GATE 1 không phụ thuộc plan này.

- **Ngã ba → `WARN:turn_l` / `WARN:turn_r` (mục B plan-ver2)**: `heritech_robot/src/main.cpp` — `checkJunction()` (mới) đọc `readJunctionType()` (thư viện: 0=None, 1=Left, 2=Right, 3=T/Cross, 4=Unknown) chỉ khi đang `FOLLOW_LINE`, xác nhận `JUNCTION_CONFIRM_FRAMES=3` lần đọc liên tiếp cùng type (chống nhiễu 1 lần đọc), gửi ĐÚNG 1 lần `WARN:turn_l`/`WARN:turn_r` rồi latch + rearm `JUNCTION_REARM_MS=500` (phải về type 0/4 + hết thời gian chờ mới báo lại). Robot KHÔNG dừng — app đã sẵn toast + TTS. `config.h` thêm 2 hằng số trên. Log `[JUNC]` mỗi lần gửi để đếm trên tuyến thật (chống trùng). Build sạch qua PlatformIO (RAM 47.9%, Flash 46.9%).
- **Hiệu chỉnh Line Tracer tại hiện trường (giữ BTN_UP ≥2s, không chặn lệnh)**: `heritech_robot/src/main.cpp` `checkButton()` — state machine non-blocking: giữ BTN_UP ≥`CALIB_HOLD_MS` (2s) khi robot IDLE → bíp 1000Hz + `sensors.calibrateBegin()` (→ `MiniR4_MXLineTracer::startCalibration()`, đo min/max tại sàn thật) → quét 2s (`CALIB_SWEEP_MS`) → tự `calibrateEnd()` + bíp 1500Hz; log `[CALIB] START`/`[CALIB] DONE`. Passthrough `calibrateBegin()/calibrateEnd()` thêm ở `sensor_manager.h/.cpp`. Giải pháp thay cho laser (quyết định team: bỏ laser khỏi mục B).
- **[LINE] debug không đổi hành vi (phục vụ xác nhận B1 — giữ nguyên V3)**: `heritech_robot/src/main.cpp` `handleFollowLine()` — mỗi 2s in `[LINE] err= w= junc=` (độ lệch centroid ±4.5, số kênh thấy line 1–10, type ngã ba) để xác nhận bám line ổn định như V3 và canh nhiễu ngã ba trước khi chạy tuyến thật. KHÔNG sửa logic PID.
- **`WRO2026_B3_Movement_Code.md` (mới, thư mục gốc)**: tổng hợp TOÀN BỘ code di chuyển để đọc kiểm tra — `config.h` (hằng số), `sensor_manager.h/.cpp` (line tracer + calibrate), `motor_control.h/.cpp` (PID bám line), `main.cpp` (`handleFollowLine`, `checkJunction`, calibration trong `checkButton`, `WAIT_CLEAR`/auto-resume, `handleAtNode`), bảng giao thức BLE di chuyển; kèm lộ trình thực test 5 bước (calibration → test bàn → tuyến thật từng phần 3a–3d → tour đầy đủ + đo số liệu GATE 1), bảng hiệu chỉnh an toàn và troubleshooting nhanh.

### Fixed
- **Cảm biến cử chỉ không nhận + WARN:person giả 3 lần lúc khởi động + sai cổng I2C**: `heritech_robot/src/config.h` — `I2C_CH_GESTURE` 3 → **1** (cổng vật lý I2C2; map cổng↔kênh MUX: I2C1=0, I2C2=1, I2C3=2, I2C4=3 trong `MatrixMiniR4.h`) cho khớp dây cắm. `main.cpp` — (1) PIR cần ~30-60s ổn định sau khi bật nguồn, trong lúc đó module tự phát xung HIGH giả dù không có người, cộng thêm `lastPIRWarn` khởi tạo 0 → cảnh báo bắn ngay khi BLE vừa kết nối rồi lặp mỗi 3s → đúng 3 lần lúc khởi động: thêm cửa sổ `PIR_WARMUP_MS=60000` (bỏ qua PIR trong 60s đầu) + debounce `PIR_DEBOUNCE_MS=400` (chỉ tin PIR khi HIGH liên tục ≥400ms, lọc xung nhiễu ngắn); (2) gesture: retry init PAJ7620 (`retryGestureInit()`, `GESTURE_REINIT_INTERVAL_MS=2000`) trước đây chỉ chạy trong `checkGesture()` — vốn bị gate sau `if (!ble.isConnected()) return;` → nếu robot bật nguồn lâu trước khi điện thoại kết nối thì sensor cử chỉ không bao giờ được init lại; giờ gọi thêm trong nhánh BLE chưa kết nối. `checkPIR()` giữ log `[PIR] raw:` để phân biệt lỗi phần cứng (raw luôn HIGH khi không người). Build qua PlatformIO (RAM 47.9%, Flash 46.4%).
- **Mọi lệnh app → robot qua BLE không bao giờ tới đích (gốc rễ của "SOS trên app không báo")**: `heritage-buddy-app/src/lib/bluetooth.ts` — `sendCommand()` gửi `btoa(cmd)` **thiếu ký tự kết thúc `\n`**, trong khi firmware chỉ hoàn tất 1 message khi gặp `\n`/`\r` trong `BLEHandler::onRXWritten()` (`heritech_robot/src/ble_handler.cpp`). Kết quả: START, SOS, NODE_DONE, RESUME, VOICE_NEXT... kẹt vĩnh viễn trong `_rxBuffer`, robot không bao giờ xử lý. Giờ gửi `btoa(cmd + "\n")` — robot nhận đủ mọi lệnh; SOS từ app giờ phản hồi giống hệt giữ switch vật lý.
- **Nút SOS trên app không có phản hồi khi bấm**: `heritage-buddy-app/src/components/robot-interaction-overlay.tsx` — trước đây app chỉ `sendCommand("SOS")` rồi chờ robot xác nhận `STATUS:sos`; BLE ngắt hoặc lệnh không tới (lỗi thiếu `\n` ở trên) → không có gì hiện. Giờ khi hoàn tất giữ ≥2s, app **kích hoạt SOS cục bộ ngay**: hiện banner SOS + rung + đọc giọng `sos.spoken` + gửi lệnh `SOS` — không phụ thuộc round-trip BLE.
- **Cảm biến cử chỉ bỏ qua vẫy tay (chỉ nhận vuốt trái/phải)**: `heritech_robot/src/main.cpp` — `checkGesture()` chỉ xử lý mã 1 (Right) và 2 (Left), nhưng `MatrixGesture::getGesture()` trả 9 (Wave), 10 (WaveSlowlyLeftRight), 13 (WaveSlowlyDisorder) khi khách vẫy tay → log `[GESTURE] raw=9` rồi bỏ qua. Giờ vẫy tay (9/10/13) = "đi tiếp" như vuốt phải, gửi `GESTURE:SWIPE_RIGHT`; vuốt trái (2) giữ nguyên. (Lưu ý: `getGesture()` có `delay(GES_QUIT_TIME)` ~1s khi phát hiện Wave — nằm trong thư viện, chưa đụng.)
- **Nhận diện ký hiệu tay trên điện thoại: "Camera chưa sẵn sàng" + "Chọn ảnh từ máy" không khả dụng**: `heritage-buddy-app/src/app/gesture-recognition.tsx` — màn hình chỉ viết cho web: trên native render `BrowserCameraPreview` (component rỗng) nên `capture()` ném "Camera chưa sẵn sàng", còn `pickBrowserImage()` từ chối vì không có `document`. Giờ tách nền tảng: native dùng `CameraNativePreview` (expo-camera, đã cài sẵn) + `expo-image-picker` (mới cài, user đã duyệt) cho "Chọn ảnh từ máy"; sửa `capture()` phải `await` (native trả `Promise<string>`); bỏ import chết `SAMPLE_JPEG_BASE64`/`jpegBase64ToInput` (xóa `src/data/gesture-sample.ts`).
- **Nút reset (↺) đè lên nút SOS trên bản đồ**: `heritage-buddy-app/src/app/museum-map.tsx` — reset FAB đặt `bottom:128` phải, nút SOS (overlay) cao `insets.bottom+96` → trên thiết bị có bottom inset ≥32px (iOS home indicator) hai nút chồng nhau. Chuyển reset FAB sang trái (soi gương nút 🤟, `bottom:190`) — không bao giờ va chạm SOS.
- **tsc toàn bộ project không clean (tồn đọng từ commit trước)**: `heritage-buddy-app/src/lib/llm.ts` — `LLMRequest`/`AudioLLMRequest` yêu cầu `language` bắt buộc nhưng caller không truyền (hàm tự điền `getLanguage()`) → để `language?: Language`; `tsconfig.json` — thêm `"types": ["jest", "node"]` cho test files (`describe/it/expect` + `process.env`).

- **Server LLM trả lời đúng ngôn ngữ (sửa khoảng trống đã ghi nhận ở mục TEST-FULL.md)**: `server/src/index.ts` — server trước đây **bỏ qua** trường `language` mà app gửi (`heritage-buddy-app/src/lib/llm.ts` gửi `language: getLanguage()` từ bước song ngữ) và system prompt hardcode "trả lời bằng tiếng Việt" → ở chế độ English, AI trả lời tiếng Việt rồi app đọc bằng giọng en-US. Giờ `buildSystemPrompt(ctx, language)` + cả 2 endpoint (`/api/ask-buddy`, `/api/ask-buddy-audio`) đọc `language` (mặc định `vi`), đổi prompt + câu hướng dẫn audio sang tiếng Anh khi `language === "en"`. `npx tsc --noEmit` server pass.
- **PIR không bao giờ đọc HIGH giả khi dây hở (chẩn đoán "WARN:person liên tục dù không có người")**: `heritech_robot/src/sensor_manager.cpp` — `pinMode(PIN_PIR, INPUT)` → `INPUT_PULLDOWN` (ESP32 hỗ trợ pull-down nội bộ): nếu dây tín hiệu đứt/hở hoặc cắm nhầm chân, chân đọc LOW thay vì nổi HIGH (trước đây dây nổi → `digitalRead` luôn HIGH → WARN:person mỗi cooldown 3s kể cả khi robot đứng yên). Thêm debug `[PIR] raw: HIGH/LOW` in mỗi 2s trong `checkPIR()` (`heritech_robot/src/main.cpp`) để phân biệt nhanh lỗi phần cứng (raw luôn HIGH khi không người) với lỗi logic.

### Added
- **expo-image-picker** (`package.json`): gói chính thức Expo SDK 57, cho phép "Chọn ảnh từ máy" trên iOS/Android ở màn nhận diện ký hiệu tay.
- **TEST-FULL.md (mới)**: danh sách kiểm thử toàn bộ dự án — app Expo (onboarding song ngữ, map 13 node, video node, chat "Hỏi Buddy" STT→LLM→TTS, nhận diện ký hiệu tay on-device), server Express (health/ask-buddy/ask-buddy-audio), tương tác BLE tóm tắt, accessibility, chỉ tiêu số đo hiệu năng (LLM round-trip, STT end-to-end, gesture recognition, BLE connect) và GATE kết luận. Bổ sung cho TEST-INTERACTION.md (chuyên sâu robot↔app); ghi nhận các lỗi tsc có sẵn (use-voice-assistant thiếu `language`, test files thiếu jest types) và khoảng trống song ngữ LLM (server chưa đọc trường `language`).

### Merged
- **Merge branch `feature/edge-ai-recognition` into `master`** (clean merge, 0 conflicts): tích hợp tính năng nhận diện ký hiệu tay on-device — route `gesture-recognition` (camera web + chọn ảnh + ảnh mẫu), model VSL int8 chạy trong JS (`src/ml/runtime.ts`, `sign.ts`, `sign-model.ts`), `src/lib/browser-camera.ts`, `src/components/BrowserCameraPreview.tsx`, ảnh mẫu `assets/images/gesture-sample-a.jpg`, nút truy cập nhanh 🤟 trên bản đồ, `testID` cho màn hình onboarding/selection. Giữ nguyên phần dev infra của master (`llm.ts` auto-detect backend URL, `scripts/start-dev.cmd`/`start-server.cmd`, `.env.example`, `WRO2026_B3_LineFollowing_Turns.md`).

### Changed
- **Dev infra — backend URL tự dò + khởi động ổn định**: `heritage-buddy-app/src/lib/llm.ts` tự dò IP máy chạy server từ `Constants.expoConfig.hostUri` (thứ tự: hostUri → `EXPO_PUBLIC_BACKEND_URL` → `http://localhost:3000`) nên hết cảnh IP LAN cũ khi DHCP đổi; `.env` cập nhật IP hiện tại (192.168.1.33) + ghi chú không cần sửa tay; `.env.example` cập nhật ghi chú; `scripts/start-server.cmd` (mới) tự khởi động lại server khi crash; `scripts/start-dev.cmd` (mới) bật server + Expo chỉ với một lệnh. Lưu ý: `npx tsc --noEmit` tiếp tục treo trong môi trường này (đã biết); `npx expo lint` pass 0 lỗi.
- **plan-ver2.md**: cập nhật trạng thái tiến độ — tick `[x]` 5/6 mục A (phần mềm & tương tác) đã xong code: giao thức (bảng tín hiệu mục 4 + CHANGELOG), firmware tương tác (`WARN:person`/`WAIT_CLEAR`/auto-resume/timeout 10s/`STATUS:auto_resumed`/`motors.stop()` khi mất BLE), app loa ngoài, app WARN UI (banner không nút + toast `WARN:turn_*` + STATUS), app SOS (nút giữ ≥2s); mục "Test bàn" (A) chưa tick — chờ kiểm tra phần cứng. Đồng thời thống nhất ngưỡng SOS theo code thực tế: switch vật lý long-press **≥10s** (`SOS_HOLD_MS=10000`) còn nút app giữ **≥2s** (trước ghi "≥2s" cho cả hai); cập nhật ở bảng hạng mục (mục 2), sơ đồ kiến trúc (mục 3), hành vi SOS (mục 4) và rủi ro (mục 8).
- **TEST-INTERACTION.md**: đồng bộ ngưỡng SOS với firmware — test 4.3 "Giữ Miniature Switch ≥2s" → **≥10s**, test 4.4 "Nhấn switch <2s" → **<10s**; thay test 2.7 lỗi thời (mô tả hành vi nút DOWN cũ "nhấn=dừng, nhả=START lại tour") bằng hành vi switch hiện tại (nhấn ngắn → `SWITCH_PRESS` mở "Hỏi Buddy", giữ ≥10s → SOS).

### Added
- **Song ngữ (mục E plan-ver2) — Bước 1: nền tảng i18n**:
  - `heritage-buddy-app/src/types/language.ts` (mới): `type Language = "vi" | "en"`.
  - `heritage-buddy-app/src/store/language.ts` (mới): Zustand store `language`/`setLanguage` + `persist` qua AsyncStorage (key `heritage-buddy-language`, `partialize` chỉ lưu `language`) + cờ `hydrated` set trong `onRehydrateStorage` — chống "nháy" ngôn ngữ khi mở lại app.
  - `heritage-buddy-app/src/app/_layout.tsx`: gate splash giữ nguyên tới khi font **và** ngôn ngữ rehydrate xong (`if (!fontsLoaded || !hydrated) return null`) rồi mới `SplashScreen.hideAsync()`.
  - `heritage-buddy-app/src/lib/i18n.ts` (mới): dictionary ~90 key × 2 ngôn ngữ (key set `vi`/`en` đối xứng, TS bắt buộc đủ key); `getLanguage()` dùng `useLanguageStore.getState()` (dùng được ngoài component); `t(key, options?, language?)` hỗ trợ interpolation `{x}`; hook `useT()` reactive cho component; `pickViEn(vi, en, language?)` fallback về VI khi bản EN rỗng.
  - `heritage-buddy-app/src/lib/i18n.test.ts` + `src/store/language.test.ts` (mới, jest-expo): 11 test — đối xứng key vi/en, không key rỗng, `t()` đúng ngôn ngữ + interpolation + fallback, `pickViEn` fallback, store rehydrate.
  - `jest.config.js` + `jest.setup.js` (mới): preset `jest-expo` + mock `@react-native-async-storage/async-storage` cho test.
  - Ghi chú: `npx tsc --noEmit` tiếp tục treo trong môi trường này (đã thử tsconfig thu hẹp phạm vi) — không phải lỗi code; `npx expo lint` pass (0 lỗi, 2 warning cũ).
- **Song ngữ (mục E plan-ver2) — Bước 2: dữ liệu**:
  - `heritage-buddy-app/src/types/museum-map.ts`: `MapNode` thêm `titleEn`/`descriptionEn`/`funFactEn` (bắt buộc — ép đủ bản dịch) + `videoSourceEn?` (để trống hiện tại, sau này chỉ cần điền link video tiếng Anh); `MuseumSection` thêm `nameEn`.
  - `heritage-buddy-app/src/data/museum-map.ts`: dịch đủ 13 hiện vật × 3 trường (title/description/funFact) + 4 tên khu sang tiếng Anh.
  - `heritage-buddy-app/src/lib/contextBuilder.ts`: context gửi LLM giờ dùng `pickViEn` theo ngôn ngữ đang chọn (khi EN có bản dịch → dùng EN, thiếu → fallback VI tránh gửi context trống); phần museum/no-node dùng `t()` từ dictionary i18n thay chuỗi hardcode.
  - `jest.config.js` + `jest.setup.js`: thêm `moduleNameMapper` cho alias `@/*` → `./heritage-buddy-app/src/*` (fix test `i18n.test.ts` lỗi "Cannot find module '@/store/language'") + `/* global jest */` (fix lint `no-undef` ở `jest.setup.js`) — 2 lỗi tồn đọng từ Bước 1 khiến `npx jest`/`npx expo lint` chưa sạch; giờ `npx jest` 11/11 pass, `npx expo lint` 0 lỗi.
- **Song ngữ (mục E plan-ver2) — Bước 3: giọng nói & LLM theo ngôn ngữ**:
  - `heritage-buddy-app/src/lib/tts.ts`: `speak()` và `useTTS().play` giờ đọc ngôn ngữ từ store (`getLanguage()`) và chọn giọng qua `resolveVoice()` dùng `Speech.getAvailableVoicesAsync()` (cache 1 lần) — tìm voice khớp `vi-*`/`en-*`; không có → gọi `Speech.speak` với language tag (`vi-VN`/`en-US`) dùng giọng mặc định, không crash. Bọc try/catch toàn bộ.
  - `heritage-buddy-app/src/lib/speech.ts`: config STT (`lang`) đọc ngôn ngữ tại thời điểm start qua `getSpeechConfig()` (`vi-VN`/`en-US`); giữ nguyên wake word / `detectWakeWord` / listener API.
  - `heritage-buddy-app/src/lib/llm.ts`: thêm `language` vào `LLMRequest`/`AudioLLMRequest`; `askBuddy`/`askBuddyWithAudio` tự gửi `language: getLanguage()` trong body; chuỗi lỗi fallback chuyển sang `t()` theo ngôn ngữ (`llm.connErr`/`llm.connectFailed`/`llm.unknownError`/`llm.networkErrorTemplate`/`llm.hearFailed`).
  - `heritage-buddy-app/server/index.js`: `SYSTEM_PROMPT` → `PROMPTS.vi/en` (system + contextLabels + contextTitle + questionLabel + audioInstruction); thêm `ERROR_STRINGS.vi/en` (noQuestion, notConfigured, genericError, noAudio, audioTooShort, geminiError); `getLanguage(req)` mặc định `vi` khi client không gửi (tương thích ngược); `buildContextBlock`/`buildAudioPrompt` tham số hóa ngôn ngữ; cả 2 route `/api/ask-buddy` và `/api/ask-buddy-audio` dùng ngôn ngữ + chuỗi lỗi tương ứng.
- **Song ngữ (mục E plan-ver2) — Bước 4: thay chuỗi hardcode → `t(key)`**:
  - Màn hình: `app/selection.tsx` thêm **bộ chọn ngôn ngữ** (Việt/English) đặt đầu màn, chọn trước chế độ hỗ trợ, gọi `setLanguage` persist; `app/index.tsx`, `app/museum-map.tsx` (kể cả 4 `Alert` + trạng thái BLE), `app/node/[id].tsx` (dùng `pickViEn` cho title), `app/chat/[nodeId].tsx` (title + empty state + trạng thái ghi âm + banner server error), `app/celebration.tsx`.
  - Component: `robot-interaction-overlay.tsx` (WARN/SOS/STATUS text + TTS qua `getLanguage()` + accessibilityLabel; `handleWarn`/`handleStatus`/`clearWarn` bọc `useCallback` để sạch `exhaustive-deps`), `ChatBubble.tsx`, `ChatHeader.tsx`, `MicButton.tsx`, `TypingIndicator.tsx`, `map-node.tsx` (title + accessibilityLabel theo `pickViEn`/`mapNode.*`), `section-banner.tsx` (tên khu `pickViEn`).
  - Hook: `use-voice-assistant.ts` — `NAV_KEYWORDS` thêm từ EN ("keep going", "move on", "next stop"...); các chuỗi chat hiển thị chuyển sang `t()` (`va.*`).
  - Kiểm tra: `npx expo lint` pass (0 lỗi, 2 warning cũ), `npx jest` 11/11 pass. `npx tsc --noEmit` treo trong môi trường này (đã ghi chú, không phải lỗi code).
- **Song ngữ (mục E plan-ver2) — Bước 5: hoàn thiện docs**:
  - `TEST-INTERACTION.md`: thêm mục 9 (Song ngữ — chọn tiếng Anh) gồm 7 test — bộ chọn ngôn ngữ + persist, UI/nội dung/bản đồ tiếng Anh, TTS `en-US` qua cảnh báo BLE, STT `en-US`, LLM trả lời tiếng Anh, và test quay về VI giữ nguyên hành vi cũ; đánh số lại mục 9→10 (chỉ tiêu số) và 10→11 (kết luận).
  - `plan-ver2.md` mục E: đánh dấu `[x]` song ngữ đã làm (tham chiếu `PLAN-BILINGUAL.md`), giữ nguyên phần ký hiệu tay (P1 — chỉ nêu nếu không kịp).
- **`PLAN-BILINGUAL.md`**: tạo plan chi tiết cho tính năng song ngữ tiếng Việt + tiếng Anh (mục E plan-ver2) sau khi rà soát code và thống nhất phạm vi — dịch toàn bộ UI + nội dung hiện vật/bản đồ + cảnh báo (banner + TTS); ngôn ngữ chọn 1 lần ở onboarding (màn `selection.tsx`) và persist qua AsyncStorage, không có nút đổi giữa phiên; giữ nguyên mọi thao tác hiện tại (không đụng wake word "Hey Buddy"); video thuyết minh tạm giữ bản tiếng Việt, data thêm `videoSourceEn?` rỗng để sau chỉ cần điền link video tiếng Anh. Plan gồm 5 bước: nền tảng (store language + gate rehydrate + `lib/i18n.ts` với `t()`/`getLanguage()`/`pickViEn`), dịch dữ liệu 13 hiện vật + 4 khu, TTS/STT/LLM theo ngôn ngữ (kèm `resolveVoice` fallback), thay chuỗi hardcode → `t(key)` theo checklist đã rà soát, và hoàn thiện (test đối xứng dictionary, CHANGELOG, TEST-INTERACTION, lint/jest/tsc).

### Removed
- **Dashboard bảo tàng (mục D plan-ver2)**: revert commit `7b52eec` theo yêu cầu — gỡ toàn bộ `heritage-buddy-app/dashboard/index.html`, `src/lib/backend.ts`, `src/lib/dashboard.ts`, các endpoint `/api/sos`, `/api/robot-status`, `/api/dashboard` ở `server/index.js`, cơ chế app tự báo cáo (overlay + use-robot-connection) và mục ignore `server/data/` trong `.gitignore`. `src/lib/llm.ts` quay về bản tự chứa fetch-fallback. `plan-ver2.md` mục D về lại trạng thái "chỉ làm nếu còn thời gian"; `TEST-INTERACTION.md` bỏ test 2.8, 4.10–4.11 và metric 9.6.

### Fixed
- **heritage-buddy-app/src/hooks/use-voice-assistant.ts + lib/speech.ts + app/node/[id].tsx**: thu âm bằng switch "lúc được lúc không" và mic tự mở hỏi tiếp không bắt được câu hỏi. Trên máy thật đường thu âm thực tế là STT (`expo-speech-recognition`, không phải expo-audio recorder). Sửa 6 nguyên nhân: (1) không đóng STT khi gửi câu hỏi → mic vẫn mở trong lúc Buddy trả lời, thu cả giọng Buddy và lần mở sau bị lỗi `busy` — giờ `stopListening()` + `resetSTT()` ngay khi submit; (2) `beginVoiceInput` nuốt lỗi STT → giờ dùng `startListeningWithWait()` (chờ sự kiện `start`/`error`, timeout) + retry 1 lần + fallback sang ghi âm expo-audio; (3) beep phát cùng lúc STT start → đua audio focus — giờ beep rồi đợi ~700ms mới mở mic; (4) màn hình node vẫn mounted phía dưới chat nên bấm switch ở chat mở thêm màn chat trùng (gửi câu hỏi 2 lần) — guard bằng `usePathname()` chỉ mở chat khi đang ở màn hình node; (5) auto-mở mic quá sớm sau TTS (engine chưa nhả audio) — giờ đợi 1s; (6) ngắt TTS giữa chừng thì `Speech.stop()` không gọi `onDone` → `inputLockRef` kẹt `true` → câu hỏi kế bị drop — giờ reset khóa khi ngắt. Thêm watchdog: đang "listening" mà engine chết im lặng (không kết quả) → hiện lỗi để thử lại thay vì kẹt.
- **heritage-buddy-app/src/lib/voice-recorder.ts + hooks/use-voice-assistant.ts**: ghi âm "không nghe rõ" trên Android — preset `LOW_QUALITY` ghi `.3gp` (AMR-NB) nhưng app gửi kèm `mimeType: "audio/m4a"` nên Gemini không giải mã được; giờ dùng AAC ADTS + nguồn `voice_recognition` (44.1kHz/mono) và `getRecordingMimeType()` trả đúng `audio/aac` trên Android. Đồng thời bật `isMeteringEnabled` và chỉ auto-dừng khi đã "nghe thấy" tiếng nói (`heardSpeech`) — trước đây metering luôn `null` khiến logic auto-stop cắt ghi âm sau đúng 3 giây dù có nói.
- **heritech_robot/src/config.h + main.cpp**: cử chỉ "đi tiếp" không bị WARN:person chặn ở mức xử lý, nhưng khách vẫy tay trước robot → PIR bắt chuyển động ngay sau → robot vừa rời node đã dừng lại (WAIT_ACK), trông như gesture không ăn. Thêm `PIR_GRACE_AFTER_LEAVE_MS=4000` — bỏ qua PIR trong lúc robot rời node (`NODE_DONE`/`NEXT_NODE`/`VOICE_NEXT`) và sau auto-resume để robot kịp di chuyển thay vì dừng liên tục.

### Changed
- **heritech_robot/src/main.cpp + state_machine.\* + config.h + heritage-buddy-app/src/types/robot.ts**: bỏ vòng ACK — khách khiếm thị không tự biết người cản đã đi chưa, nên bắt bấm nút "Đã hiểu" là vô nghĩa. Giờ state `WAIT_ACK` → `WAIT_CLEAR`: sau `WARN:person` robot dừng và **tự đi tiếp khi PIR im lặng liên tục ≥2s** (`PIR_CLEAR_CONFIRM_MS`) → `STATUS:auto_resumed`; an toàn hết `WARN_CLEAR_TIMEOUT_MS` (10s) mà PIR vẫn báo → vẫn tự chạy tiếp (log `[STATE] Path clear -> auto resume` / `[STATE] WARN timeout -> auto resume`). Xóa lệnh `ACK` khỏi giao thức app→robot; app banner WARN không còn nút bấm (đã không có từ commit a392591 — đây là lần hoàn thiện đúng thiết kế). Cập nhật `plan-ver2.md` + `TEST-INTERACTION.md` (mục 3, 7, 8, 9, GATE 1) theo hành vi mới.
- **heritech_robot/src/config.h + main.cpp**: `SOS_HOLD_MS` 5000 → 10000 — giữ công tắc ≥10s mới kích hoạt SOS để không lẫn với nhấn ngắn mở "Hỏi Buddy" (cập nhật comment + log `[SWITCH] Long press >= 10s -> SOS`).
- **heritage-buddy-app/src/lib/sound.ts + assets/sounds/record-beep.wav + hooks/use-voice-assistant.ts**: khi bấm "nhấn để nói" hoặc dùng công tắc, điện thoại phát tiếng "bíp bíp" (`playRecordBeep()`) để người xung quanh biết đang bắt đầu ghi âm; beep phát bằng `createAudioPlayer` (tạo lười) nên không làm hỏng luồng ghi âm/STT.
- **heritage-buddy-app/src/hooks/use-voice-assistant.ts**: beep "đang nghe" chuyển từ trước delay 700ms sang phát SAU khi `startListeningWithWait()` xác nhận STT thực sự mở (cả lần retry và fallback ghi âm) — người dùng nghe bíp là mic đã mở, không nói vào mic chưa sẵn sàng (trước đây bíp phát trước khi mic mở nên không có ý nghĩa báo "được phép nói").
- **heritech_robot/src/sensor_manager.h + sensor_manager.cpp + main.cpp**: gesture sensor (PAJ7620) "không nhận/gửi tín hiệu" — `begin()` được gọi nhưng return value bị bỏ qua; nếu sensor không detect trên kênh MUX mặc định (I2C channel 3) thì `getGesture()` im lặng trả 0 mãi, không có recovery. Giờ `tryGestureOnChannel()` kiểm tra `begin()==ERR_OK` kèm log kênh, `initGestureSensor()` thử kênh cấu hình trước rồi quét cả 8 kênh MUX (delay 50ms giữa các lần), và `checkGesture()` poll `reinitGesture()` mỗi 2s (`GESTURE_REINIT_INTERVAL_MS`) khi sensor chưa ready — khi đã ready vẫn dùng `MatrixGesture::eGestureRight`/`eGestureLeft` (1=Right, 2=Left) gửi `GESTURE:SWIPE_RIGHT/LEFT`. Build verified via PlatformIO (RAM 47.8%, Flash 46.2%).


### Changed
- **heritage-buddy-app/src/app/node/[id].tsx + chat/[nodeId].tsx**: công tắc vật lý giờ hoạt động 2 bước — bấm lần 1 chỉ mở màn hình "Hỏi Buddy" (phần câu hỏi), bấm lần nữa mới kích hoạt ghi âm/gửi câu hỏi (bỏ cơ chế `autoMic=1` tự ghi âm ngay khi mở chat).
- **heritage-buddy-app/src/hooks/use-voice-assistant.ts + use-gesture-navigation.ts + app/node/[id].tsx**: mọi tín hiệu "đi tiếp" (ghi âm/giọng nói, cử chỉ, nút trên màn hình) giờ đưa người dùng về bản đồ (`/museum-map`) trước — robot tự di chuyển, và khi nhận `NODE_START:<idx>` từ robot thì app mới tự mở màn hình nội dung node tương ứng.
- **heritage-buddy-app/src/hooks/use-robot-connection.ts**: xử lý `NODE_START` → tự điều hướng đến màn hình node (có guard tránh mở lại khi đang đứng sẵn trên node đó); cử chỉ `GESTURE:SWIPE_RIGHT`/`GESTURE:SWIPE_LEFT` chỉ được ghi nhận khi app đang ở màn hình node (tránh cử chỉ cũ bị màn hình node kế dùng nhầm); thêm debounce 500ms cho `SWITCH_PRESS`.
- **heritech_robot/src/config.h + main.cpp**: thêm debounce 40ms cho công tắc vật lý (`SWITCH_DEBOUNCE_MS`) — nhiễu phím không còn tạo cạnh giả gây mất `SWITCH_PRESS` hoặc kích hoạt nhầm SOS; vẫn giữ: nhấn ngắn → `SWITCH_PRESS`, giữ ≥2s → SOS.
- **heritech_robot/src/main.cpp**: cử chỉ từ M-Vision Cam đổi từ "vuốt lên" sang "vuốt trái/phải" — `GESTURE:SWIPE_RIGHT` (getGesture()=1) & `GESTURE:SWIPE_LEFT` (getGesture()=2) đều = "đi tiếp" (trước đây check `0x04` thực chất là cử chỉ Down). App tương ứng: `use-robot-connection.ts` parse `GESTURE:SWIPE_RIGHT`/`GESTURE:SWIPE_LEFT` và đặt `swipe_right`/`swipe_left`, `types/robot.ts` cập nhật lệnh.
- **heritage-buddy-app/src/lib/llm.ts**: tự động fallback URL máy chủ — nếu không kết nối được `EXPO_PUBLIC_BACKEND_URL` (IP LAN) thì thử `http://localhost:3000` (hoạt động khi debug qua USB với `adb reverse tcp:3000 tcp:3000`). Khắc phục lỗi "không kết nối được máy chủ" khi app chạy debug cắm dây nhưng điện thoại không cùng WiFi máy tính.
- **heritech_robot/src/main.cpp**: fix cử chỉ "đi tiếp" (`VOICE_NEXT`) — trước đây firmware gửi `NODE_COMPLETE:<node kế tiếp>` nên app đánh dấu sai node đích thành "đã xong ✓" trên bản đồ; giờ lưu index node vừa hoàn thành trước khi `nextNode()` và gửi đúng index đó, node kế tiếp hiển thị đúng là node hiện tại trên map.
- **heritage-buddy-app/src/hooks/use-robot-connection.ts**: bỏ hộp thoại `Alert` yêu cầu bấm "OK" khi nhận `ALARM` (legacy PIR) — phát hiện người giờ chỉ thông báo (rung nhẹ) qua `WARN:person`, không bắt người dùng chọn/bấm gì.
- **heritage-buddy-app/src/components/robot-interaction-overlay.tsx**: nút SOS đỏ nổi không còn hiển thị trên màn hình onboarding (`/`, `/selection`) và màn hình chat (`/chat/*`) — tránh đè lên nút CTA / ô nhập câu hỏi khiến SOS và "Hỏi Buddy" bị lẫn.
- **heritage-buddy-app/src/app/node/[id].tsx + museum-map.tsx**: chừa khoảng trống bên phải (~112px) cho nút SOS đỏ nổi ở đáy màn hình node và bản đồ — nút "Hỏi Buddy"/"Đi tiếp"/"Xuất phát" không còn bị nút SOS chồng lên.
- **heritage-buddy-app/src/types/robot.ts**: bỏ `swipe_up`/`swipe_down` khỏi `GestureType` (không còn dùng).

### Added
- **AGENTS.md**: add rule that every code change MUST have a corresponding changelog entry in `CHANGELOG.md` before committing.
- **plan-ver2.md**: REWRITE as the single official prototype plan — real system stays Line Tracer + Color Sensor (from V1–V3); new real features: two-way ACK interaction, SOS, phone speaker; sign-language recognition kept as optional P1 stretch; adds WARN-type protocol table, numeric targets/GATE 1–2, 15-day timeline + team split. Deadline 2026-08-20.
- **heritech_robot/src/main.cpp**: firmware now also handles app `ACK` (→ replies `STATUS:resumed`) and `SOS` (→ stop + red LED + buzzer + `STATUS:sos`). Build verified via PlatformIO (uno_r4_wifi).
- **heritage-buddy-app/src/types/assets.d.ts**: add ambient module declarations for image assets (`*.png`/`*.jpg`/`*.jpeg`/`*.webp`/`*.gif`/`*.svg` → `ImageSourcePropType`, `*.ttf` → `string`) — fixes the 12 pre-existing `tsc --noEmit` errors in `constants/images.ts` (asset imports previously had no type declarations); the whole repo now typechecks clean (dùng `import("react-native").ImageSourcePropType` inline để giữ khai báo ambient, không biến file thành module).

### Added
- **TEST-INTERACTION.md**: full robot↔app interaction test list covering all features — preparation & room setup, BLE connect/reconnect, full tour flow, two-way `WARN:person`→ACK loop (incl. 10s timeout / auto-resume, PIR cooldown), SOS (app hold-≥2s, physical switch long-press, `RESUME` without tour reset, offline SOS), `WARN:turn_*` toasts (app-side testable via BLE UART terminal), legacy sensors (switch/gesture/voice/ALL_DONE), safety edge cases (BLE-loss auto-stop mid-run, mid-ACK drop, reconnect), speaker/TTS verification, and per-metric measurement tables (ACK <3s, banner <1s, SOS <2s, timeout 10s±1s, node accuracy) with GATE 1/2 pass criteria.
- **heritech_robot/src/main.cpp + state_machine.\* + config.h**: two-way ACK interaction & SOS on real firmware — `WARN:person` (PIR) replaces `ALARM`; robot pauses in FOLLOW_LINE → new `WAIT_ACK` state, waits `ACK` max 10s (`WARN_ACK_TIMEOUT_MS`) then auto-resumes + `STATUS:auto_resumed`; PIR suppressed while already `WAIT_ACK`; long-press ≥2s (`SOS_HOLD_MS`) on the Miniature Switch → SOS (stop + red LED + buzzer + `STATUS:sos`) vs short press → `SWITCH_PRESS`; new `RESUME` command to continue after SOS without resetting the tour (unlike `START`); safety `motors.stop()` on BLE disconnect so the robot never keeps running with a stale speed command.
- **heritage-buddy-app/src/components/robot-interaction-overlay.tsx**: new global overlay mounted in root layout that listens for `WARN:`/`STATUS:` BLE messages on every screen — full-screen `WARN:person` banner (mascot + TTS + big "Đã hiểu / Tiếp tục" ACK + "Dừng lại" STOP buttons), transient `WARN:turn_*` toast, `STATUS` confirmation toasts, SOS flow (fixed hold-≥2s button → `SOS`, banner → `RESUME`), 10.5s app-side fallback banner dismiss if BLE drops mid-loop.

### Fixed
- **heritech_robot/src/main.cpp**: restore missing `sensors.begin()` and `state.setState(RobotState::IDLE)` in active `setup()` (they were only inside the commented-out legacy block) and remove the dead commented block that referenced `Serial.begin(115200)`. This fixes BLE connection failure: uninitialized I2C sensors (line tracer/color/gesture) were read every loop once a phone connected, stalling `BLE.poll()` so the phone's GATT discovery/connection-parameter update timed out and terminated the link (confirmed via nRF Connect: "Connection parameters update failed 0x16 → CONN TERMINATE"). After the fix the app reaches `[BLE] Ready` and nRF Connect discovers the Nordic UART Service.

### Changed
- **PLAN-BLE-FIX.md**: mark Fix 1/2/4 code items as done, keep hardware/conditional items open (firmware reflash, Step-0 hardware tests, Fix 3 GATT runbook, manager.destroy last resort).
- **heritech_robot/platformio.ini**: `monitor_speed` 115200 → 9600 to match `Serial.begin(9600)` (was unreadable before).
- **heritech_robot/src/main.cpp**: add temporary connected-loop time debug log — `[LOOP] max loop time:` every 3s to catch any blocking that would stall `BLE.poll()` (GATT discovery). Also fix `platformio.ini` comment `main.cpp:120`→`:122`.
- **heritage-buddy-app/src/lib/bluetooth.ts**: BLE discovery hardening — ~500ms settle delay after `connectToDevice()` before first discovery; retry backoff 1500ms → 2500ms; wrap `services()`/`service.characteristics()` in try/catch (timeout no longer misreported as "Connection error"); stale `onDisconnected` callback now only resets state when `bleState.device === connectedDevice` (old device disconnecting late can no longer wipe a live new connection).
- **heritage-buddy-app/src/hooks/use-robot-connection.ts**: auto-retry/auto-reconnect — after a failed `connect()` or an unexpected disconnect (not a user-initiated one), automatically retry the connection after 3s, max 2 attempts; uses `connectRef` synced via `useEffect` to avoid circular `useCallback` deps and React-compiler refs-during-render lint error.

### Changed
- **plan-ver2.md**: drop AprilTag/Radar entirely — Line Tracer + Color Sensor is the official sole navigation architecture, no future-direction section (removes references to `PLAN-APRILTAG.md`, which was never committed); `WARN:person` (PIR) replaces `ALARM` as the ACK pause source; SOS = long-press ≥2s on the existing Miniature Switch + app button; explicit ACK semantics table (person pauses + 10s `WARN_ACK_TIMEOUT_MS` timeout → `STATUS:auto_resumed`; turn warnings announce-only at the junction, `WARN:node` dropped as redundant with `NODE_START`); add BLE-loss auto-stop safety (`motors.stop()` on disconnect); fix dashboard polling 2s vs <5s target contradiction; reorder stages — software & interaction first, movement second; feature freeze from 17/08; fix section numbering.
- **heritage-buddy-app/src/lib/bluetooth.ts**: BLE UART discovery retry with backoff (3 attempts) instead of a hard 3.5s delay; register `onDisconnected` only after discovery succeeds so a failed discover no longer kills a live connection.
- **heritage-buddy-app/src/store/robot.ts**: add `activeWarn`, `robotStatus`, `sosActive` + setters.
- **heritage-buddy-app/src/types/robot.ts**: add `WARN:<type>` / `STATUS:<state>` to robot→app commands and `ACK` / `SOS` / `RESUME` to app→robot commands; add `WarnType`, `RobotStatusType`.
- **heritage-buddy-app/src/hooks/use-robot-connection.ts**: parse `WARN:`/`STATUS:` messages so they no longer log as unknown commands.
- **heritage-buddy-app/server/index.js**: add `/* global __dirname */` header to fix ESLint `no-undef`.
- **package.json / package-lock.json**: add `@react-native/jest-preset` override + dev dependency for Jest compatibility.
- **.gitignore**: ignore `.vscode/` and `compile_commands.json` (local editor/build artifacts).

### Removed
- **heritech_robot/src/config.h + main.cpp**: remove TEST MODE (auto-simulated `WARN`/`NODE_START` cycle + `TEST_WARN:<type>`/`TEST_NODE` commands + `sendWarn`/`handleTestMode` helpers). Robot now relies solely on real autonomous line-following; node opening is triggered by the existing Color Sensor red detection (3 stable reads) → `NODE_START:<id>`, unchanged.
- **heritech_robot/examples/**: delete `move_20cm` test-mode example sketch.

### Docs
- **CHANGELOG_BLE_FIX.md**: keep detailed write-up of the BLE discovery fix (root cause + before/after).
- **PLAN-BLE-FIX.md**: new fix plan for the current BLE discovery timeout (robot connects but `UART service not found`) — 4 hypotheses (H1 blocking poll in old firmware / H2 discovery too early / H3 Android GATT cache / H4 monitor_speed mismatch), prioritized fixes per layer (firmware non-blocking poll + debug loop-time log, app 500ms settle delay + larger backoff + auto-retry, GATT cache runbook, `monitor_speed` 115200→9600), test matrix (10 connects, reconnect, reboot) and Definition of Done (≥9/10 success, discovery <3s). Rev 2 (2026-08-05, after audit): fix deadline 08/08→14/08 (GATE 1 per plan-ver2), correct `main.cpp:120`→`:122` and remove non-existent `delay(300)` in `handleEnd`; add Step 0 zero-code tests (monitor-speed firmware fingerprint — old FW 115200 vs new 9600 — + forget-device GATT cache check); note the `attempt x/3` log proves current retry code fails 3/3 ⇒ systematic, H2 nearly ruled out; expand Fix 2 with try/catch around `services()`/`characteristics()`, stale `onDisconnected` callback bug fix (check `bleState.device === connectedDevice`), hook-level auto-retry/auto-reconnect, and `manager.destroy()` last resort; mark reconnect test as dependent on Fix 2.
# Nhận diện ký hiệu tay trong app

- Thêm route `gesture-recognition` với camera web, chọn ảnh và ảnh mẫu kiểm thử.
- Nhúng lại model VSL int8 chạy trực tiếp trong JavaScript, không gọi server.
- Thêm nút truy cập nhanh từ bản đồ bảo tàng và hiển thị nhãn, độ tin cậy, top 3 kết quả.
- Thêm ảnh mẫu chữ A lấy từ tập test VSL để xác minh luồng end-to-end khi camera không khả dụng.
