# Changelog

## [Unreleased]

### Added
- **Song ngữ (mục E plan-ver2) — Bước 1: nền tảng i18n**:
  - `heritage-buddy-app/src/types/language.ts` (mới): `type Language = "vi" | "en"`.
  - `heritage-buddy-app/src/store/language.ts` (mới): Zustand store `language`/`setLanguage` + `persist` qua AsyncStorage (key `heritage-buddy-language`, `partialize` chỉ lưu `language`) + cờ `hydrated` set trong `onRehydrateStorage` — chống "nháy" ngôn ngữ khi mở lại app.
  - `heritage-buddy-app/src/app/_layout.tsx`: gate splash giữ nguyên tới khi font **và** ngôn ngữ rehydrate xong (`if (!fontsLoaded || !hydrated) return null`) rồi mới `SplashScreen.hideAsync()`.
  - `heritage-buddy-app/src/lib/i18n.ts` (mới): dictionary ~90 key × 2 ngôn ngữ (key set `vi`/`en` đối xứng, TS bắt buộc đủ key); `getLanguage()` dùng `useLanguageStore.getState()` (dùng được ngoài component); `t(key, options?, language?)` hỗ trợ interpolation `{x}`; hook `useT()` reactive cho component; `pickViEn(vi, en, language?)` fallback về VI khi bản EN rỗng.
  - `heritage-buddy-app/src/lib/i18n.test.ts` + `src/store/language.test.ts` (mới, jest-expo): 11 test — đối xứng key vi/en, không key rỗng, `t()` đúng ngôn ngữ + interpolation + fallback, `pickViEn` fallback, store rehydrate.
  - `jest.config.js` + `jest.setup.js` (mới): preset `jest-expo` + mock `@react-native-async-storage/async-storage` cho test.
  - Ghi chú: `npx tsc --noEmit` tiếp tục treo trong môi trường này (đã thử tsconfig thu hẹp phạm vi) — không phải lỗi code; `npx expo lint` pass (0 lỗi, 2 warning cũ).
- **Song ngữ (mục E plan-ver2) — Bước 2: dữ liệu**:
  - `heritage-buddy-app/src/types/museum-map.ts`: `MapNode` thêm `titleEn`/`descriptionEn`/`funFactEn` (bắt buộc — ép đủ bản dịch) + `videoSourceEn?` (để trống hiện tại, sau này chỉ cần điền link video tiếng Anh); `MuseumSection` thêm `nameEn`.
  - `heritage-buddy-app/src/data/museum-map.ts`: dịch đủ 13 hiện vật × 3 trường (title/description/funFact) + 4 tên khu sang tiếng Anh.
  - `heritage-buddy-app/src/lib/contextBuilder.ts`: context gửi LLM giờ dùng `pickViEn` theo ngôn ngữ đang chọn (khi EN có bản dịch → dùng EN, thiếu → fallback VI tránh gửi context trống); phần museum/no-node dùng `t()` từ dictionary i18n thay chuỗi hardcode.
  - `jest.config.js` + `jest.setup.js`: thêm `moduleNameMapper` cho alias `@/*` → `./heritage-buddy-app/src/*` (fix test `i18n.test.ts` lỗi "Cannot find module '@/store/language'") + `/* global jest */` (fix lint `no-undef` ở `jest.setup.js`) — 2 lỗi tồn đọng từ Bước 1 khiến `npx jest`/`npx expo lint` chưa sạch; giờ `npx jest` 11/11 pass, `npx expo lint` 0 lỗi.
- **`PLAN-BILINGUAL.md`**: tạo plan chi tiết cho tính năng song ngữ tiếng Việt + tiếng Anh (mục E plan-ver2) sau khi rà soát code và thống nhất phạm vi — dịch toàn bộ UI + nội dung hiện vật/bản đồ + cảnh báo (banner + TTS); ngôn ngữ chọn 1 lần ở onboarding (màn `selection.tsx`) và persist qua AsyncStorage, không có nút đổi giữa phiên; giữ nguyên mọi thao tác hiện tại (không đụng wake word "Hey Buddy"); video thuyết minh tạm giữ bản tiếng Việt, data thêm `videoSourceEn?` rỗng để sau chỉ cần điền link video tiếng Anh. Plan gồm 5 bước: nền tảng (store language + gate rehydrate + `lib/i18n.ts` với `t()`/`getLanguage()`/`pickViEn`), dịch dữ liệu 13 hiện vật + 4 khu, TTS/STT/LLM theo ngôn ngữ (kèm `resolveVoice` fallback), thay chuỗi hardcode → `t(key)` theo checklist đã rà soát, và hoàn thiện (test đối xứng dictionary, CHANGELOG, TEST-INTERACTION, lint/jest/tsc).

### Removed
- **Dashboard bảo tàng (mục D plan-ver2)**: revert commit `7b52eec` theo yêu cầu — gỡ toàn bộ `heritage-buddy-app/dashboard/index.html`, `src/lib/backend.ts`, `src/lib/dashboard.ts`, các endpoint `/api/sos`, `/api/robot-status`, `/api/dashboard` ở `server/index.js`, cơ chế app tự báo cáo (overlay + use-robot-connection) và mục ignore `server/data/` trong `.gitignore`. `src/lib/llm.ts` quay về bản tự chứa fetch-fallback. `plan-ver2.md` mục D về lại trạng thái "chỉ làm nếu còn thời gian"; `TEST-INTERACTION.md` bỏ test 2.8, 4.10–4.11 và metric 9.6.

### Fixed
- **heritage-buddy-app/src/hooks/use-voice-assistant.ts + lib/speech.ts + app/node/[id].tsx**: thu âm bằng switch "lúc được lúc không" và mic tự mở hỏi tiếp không bắt được câu hỏi. Trên máy thật đường thu âm thực tế là STT (`expo-speech-recognition`, không phải expo-audio recorder). Sửa 6 nguyên nhân: (1) không đóng STT khi gửi câu hỏi → mic vẫn mở trong lúc Buddy trả lời, thu cả giọng Buddy và lần mở sau bị lỗi `busy` — giờ `stopListening()` + `resetSTT()` ngay khi submit; (2) `beginVoiceInput` nuốt lỗi STT → giờ dùng `startListeningWithWait()` (chờ sự kiện `start`/`error`, timeout) + retry 1 lần + fallback sang ghi âm expo-audio; (3) beep phát cùng lúc STT start → đua audio focus — giờ beep rồi đợi ~700ms mới mở mic; (4) màn hình node vẫn mounted phía dưới chat nên bấm switch ở chat mở thêm màn chat trùng (gửi câu hỏi 2 lần) — guard bằng `usePathname()` chỉ mở chat khi đang ở màn hình node; (5) auto-mở mic quá sớm sau TTS (engine chưa nhả audio) — giờ đợi 1s; (6) ngắt TTS giữa chừng thì `Speech.stop()` không gọi `onDone` → `inputLockRef` kẹt `true` → câu hỏi kế bị drop — giờ reset khóa khi ngắt. Thêm watchdog: đang "listening" mà engine chết im lặng (không kết quả) → hiện lỗi để thử lại thay vì kẹt.
- **heritage-buddy-app/src/lib/voice-recorder.ts + hooks/use-voice-assistant.ts**: ghi âm "không nghe rõ" trên Android — preset `LOW_QUALITY` ghi `.3gp` (AMR-NB) nhưng app gửi kèm `mimeType: "audio/m4a"` nên Gemini không giải mã được; giờ dùng AAC ADTS + nguồn `voice_recognition` (44.1kHz/mono) và `getRecordingMimeType()` trả đúng `audio/aac` trên Android. Đồng thời bật `isMeteringEnabled` và chỉ auto-dừng khi đã "nghe thấy" tiếng nói (`heardSpeech`) — trước đây metering luôn `null` khiến logic auto-stop cắt ghi âm sau đúng 3 giây dù có nói.
- **heritech_robot/src/config.h + main.cpp**: cử chỉ "đi tiếp" không bị WARN:person chặn ở mức xử lý, nhưng khách vẫy tay trước robot → PIR bắt chuyển động ngay sau → robot vừa rời node đã dừng lại (WAIT_ACK), trông như gesture không ăn. Thêm `PIR_GRACE_AFTER_LEAVE_MS=4000` — bỏ qua PIR trong lúc robot rời node (`NODE_DONE`/`NEXT_NODE`/`VOICE_NEXT`) và sau auto-resume để robot kịp di chuyển thay vì dừng liên tục.

### Changed
- **heritech_robot/src/main.cpp + state_machine.\* + config.h + heritage-buddy-app/src/types/robot.ts**: bỏ vòng ACK — khách khiếm thị không tự biết người cản đã đi chưa, nên bắt bấm nút "Đã hiểu" là vô nghĩa. Giờ state `WAIT_ACK` → `WAIT_CLEAR`: sau `WARN:person` robot dừng và **tự đi tiếp khi PIR im lặng liên tục ≥2s** (`PIR_CLEAR_CONFIRM_MS`) → `STATUS:auto_resumed`; an toàn hết `WARN_CLEAR_TIMEOUT_MS` (10s) mà PIR vẫn báo → vẫn tự chạy tiếp (log `[STATE] Path clear -> auto resume` / `[STATE] WARN timeout -> auto resume`). Xóa lệnh `ACK` khỏi giao thức app→robot; app banner WARN không còn nút bấm (đã không có từ commit a392591 — đây là lần hoàn thiện đúng thiết kế). Cập nhật `plan-ver2.md` + `TEST-INTERACTION.md` (mục 3, 7, 8, 9, GATE 1) theo hành vi mới.
- **heritech_robot/src/config.h + main.cpp**: `SOS_HOLD_MS` 5000 → 10000 — giữ công tắc ≥10s mới kích hoạt SOS để không lẫn với nhấn ngắn mở "Hỏi Buddy" (cập nhật comment + log `[SWITCH] Long press >= 10s -> SOS`).
- **heritage-buddy-app/src/lib/sound.ts + assets/sounds/record-beep.wav + hooks/use-voice-assistant.ts**: khi bấm "nhấn để nói" hoặc dùng công tắc, điện thoại phát tiếng "bíp bíp" (`playRecordBeep()`) để người xung quanh biết đang bắt đầu ghi âm; beep phát bằng `createAudioPlayer` (tạo lười) nên không làm hỏng luồng ghi âm/STT.
- **heritage-buddy-app/src/hooks/use-voice-assistant.ts**: beep "đang nghe" chuyển từ trước delay 700ms sang phát SAU khi `startListeningWithWait()` xác nhận STT thực sự mở (cả lần retry và fallback ghi âm) — người dùng nghe bíp là mic đã mở, không nói vào mic chưa sẵn sàng (trước đây bíp phát trước khi mic mở nên không có ý nghĩa báo "được phép nói").
- **heritech_robot/src/sensor_manager.h + sensor_manager.cpp + main.cpp**: gesture sensor (PAJ7620) "không nhận/gửi tín hiệu" — `begin()` được gọi nhưng return value bị bỏ qua; nếu sensor không detect trên kênh MUX mặc định (I2C channel 3) thì `getGesture()` im lặng trả 0 mãi, không có recovery. Giờ `tryGestureOnChannel()` kiểm tra `begin()==ERR_OK` kèm log kênh, `initGestureSensor()` thử kênh cấu hình trước rồi quét cả 8 kênh MUX (delay 50ms giữa các lần), và `checkGesture()` poll `reinitGesture()` mỗi 2s (`GESTURE_REINIT_INTERVAL_MS`) khi sensor chưa ready — khi đã ready vẫn dùng `MatrixGesture::eGestureRight`/`eGestureLeft` (1=Right, 2=Left) gửi `GESTURE:SWIPE_RIGHT/LEFT`. Build verified via PlatformIO (RAM 47.8%, Flash 46.2%).


### Changed
- **heritage-buddy-app/src/app/node/[id].tsx + chat/[nodeId].tsx**: công tắc vật lý giờ hoạt động 2 bước — bấm lần 1 chỉ mở màn hình "Hỏi Buddy" (phần câu hỏi), bấm lần nữa mới kích hoạt ghi âm/gửi câu hỏi (bỏ cơ chế `autoMic=1` tự ghi âm ngay khi mở chat).
- **heritage-buddy-app/src/hooks/use-voice-assistant.ts + use-gesture-navigation.ts + app/node/[id].tsx**: mọi tín hiệu "đi tiếp" (ghi âm/giọng nói, cử chỉ, nút trên màn hình) giờ đưa người dùng về bản đồ (`/museum-map`) trước — robot tự di chuyển, và khi nhận `NODE_START:<idx>` từ robot thì app mới tự mở màn hình nội dung node tương ứng.
- **heritage-buddy-app/src/hooks/use-robot-connection.ts**: xử lý `NODE_START` → tự điều hướng đến màn hình node (có guard tránh mở lại khi đang đứng sẵn trên node đó); cử chỉ `GESTURE:SWIPE_RIGHT`/`GESTURE:SWIPE_LEFT` chỉ được ghi nhận khi app đang ở màn hình node (tránh cử chỉ cũ bị màn hình node kế dùng nhầm); thêm debounce 500ms cho `SWITCH_PRESS`.
- **heritech_robot/src/config.h + main.cpp**: thêm debounce 40ms cho công tắc vật lý (`SWITCH_DEBOUNCE_MS`) — nhiễu phím không còn tạo cạnh giả gây mất `SWITCH_PRESS` hoặc kích hoạt nhầm SOS; vẫn giữ: nhấn ngắn → `SWITCH_PRESS`, giữ ≥2s → SOS.
- **heritech_robot/src/main.cpp**: cử chỉ từ M-Vision Cam đổi từ "vuốt lên" sang "vuốt trái/phải" — `GESTURE:SWIPE_RIGHT` (getGesture()=1) & `GESTURE:SWIPE_LEFT` (getGesture()=2) đều = "đi tiếp" (trước đây check `0x04` thực chất là cử chỉ Down). App tương ứng: `use-robot-connection.ts` parse `GESTURE:SWIPE_RIGHT`/`GESTURE:SWIPE_LEFT` và đặt `swipe_right`/`swipe_left`, `types/robot.ts` cập nhật lệnh.
- **heritage-buddy-app/src/lib/llm.ts**: tự động fallback URL máy chủ — nếu không kết nối được `EXPO_PUBLIC_BACKEND_URL` (IP LAN) thì thử `http://localhost:3000` (hoạt động khi debug qua USB với `adb reverse tcp:3000 tcp:3000`). Khắc phục lỗi "không kết nối được máy chủ" khi app chạy debug cắm dây nhưng điện thoại không cùng WiFi máy tính.
- **heritech_robot/src/main.cpp**: fix cử chỉ "đi tiếp" (`VOICE_NEXT`) — trước đây firmware gửi `NODE_COMPLETE:<node kế tiếp>` nên app đánh dấu sai node đích thành "đã xong ✓" trên bản đồ; giờ lưu index node vừa hoàn thành trước khi `nextNode()` và gửi đúng index đó, node kế tiếp hiển thị đúng là node hiện tại trên map.
- **heritage-buddy-app/src/hooks/use-robot-connection.ts**: bỏ hộp thoại `Alert` yêu cầu bấm "OK" khi nhận `ALARM` (legacy PIR) — phát hiện người giờ chỉ thông báo (rung nhẹ) qua `WARN:person`, không bắt người dùng chọn/bấm gì.
- **heritage-buddy-app/src/components/robot-interaction-overlay.tsx**: nút SOS đỏ nổi không còn hiển thị trên màn hình onboarding (`/`, `/selection`) và màn hình chat (`/chat/*`) — tránh đè lên nút CTA / ô nhập câu hỏi khiến SOS và "Hỏi Buddy" bị lẫn.
- **heritage-buddy-app/src/app/node/[id].tsx + museum-map.tsx**: chừa khoảng trống bên phải (~112px) cho nút SOS đỏ nổi ở đáy màn hình node và bản đồ — nút "Hỏi Buddy"/"Đi tiếp"/"Xuất phát" không còn bị nút SOS chồng lên.
- **heritage-buddy-app/src/types/robot.ts**: bỏ `swipe_up`/`swipe_down` khỏi `GestureType` (không còn dùng).

### Added
- **AGENTS.md**: add rule that every code change MUST have a corresponding changelog entry in `CHANGELOG.md` before committing.
- **plan-ver2.md**: REWRITE as the single official prototype plan — real system stays Line Tracer + Color Sensor (from V1–V3); new real features: two-way ACK interaction, SOS, phone speaker; sign-language recognition kept as optional P1 stretch; adds WARN-type protocol table, numeric targets/GATE 1–2, 15-day timeline + team split. Deadline 2026-08-20.
- **heritech_robot/src/main.cpp**: firmware now also handles app `ACK` (→ replies `STATUS:resumed`) and `SOS` (→ stop + red LED + buzzer + `STATUS:sos`). Build verified via PlatformIO (uno_r4_wifi).
- **heritage-buddy-app/src/types/assets.d.ts**: add ambient module declarations for image assets (`*.png`/`*.jpg`/`*.jpeg`/`*.webp`/`*.gif`/`*.svg` → `ImageSourcePropType`, `*.ttf` → `string`) — fixes the 12 pre-existing `tsc --noEmit` errors in `constants/images.ts` (asset imports previously had no type declarations); the whole repo now typechecks clean (dùng `import("react-native").ImageSourcePropType` inline để giữ khai báo ambient, không biến file thành module).

### Added
- **TEST-INTERACTION.md**: full robot↔app interaction test list covering all features — preparation & room setup, BLE connect/reconnect, full tour flow, two-way `WARN:person`→ACK loop (incl. 10s timeout / auto-resume, PIR cooldown), SOS (app hold-≥2s, physical switch long-press, `RESUME` without tour reset, offline SOS), `WARN:turn_*` toasts (app-side testable via BLE UART terminal), legacy sensors (switch/gesture/voice/ALL_DONE), safety edge cases (BLE-loss auto-stop mid-run, mid-ACK drop, reconnect), speaker/TTS verification, and per-metric measurement tables (ACK <3s, banner <1s, SOS <2s, timeout 10s±1s, node accuracy) with GATE 1/2 pass criteria.
- **heritech_robot/src/main.cpp + state_machine.\* + config.h**: two-way ACK interaction & SOS on real firmware — `WARN:person` (PIR) replaces `ALARM`; robot pauses in FOLLOW_LINE → new `WAIT_ACK` state, waits `ACK` max 10s (`WARN_ACK_TIMEOUT_MS`) then auto-resumes + `STATUS:auto_resumed`; PIR suppressed while already `WAIT_ACK`; long-press ≥2s (`SOS_HOLD_MS`) on the Miniature Switch → SOS (stop + red LED + buzzer + `STATUS:sos`) vs short press → `SWITCH_PRESS`; new `RESUME` command to continue after SOS without resetting the tour (unlike `START`); safety `motors.stop()` on BLE disconnect so the robot never keeps running with a stale speed command.
- **heritage-buddy-app/src/components/robot-interaction-overlay.tsx**: new global overlay mounted in root layout that listens for `WARN:`/`STATUS:` BLE messages on every screen — full-screen `WARN:person` banner (mascot + TTS + big "Đã hiểu / Tiếp tục" ACK + "Dừng lại" STOP buttons), transient `WARN:turn_*` toast, `STATUS` confirmation toasts, SOS flow (fixed hold-≥2s button → `SOS`, banner → `RESUME`), 10.5s app-side fallback banner dismiss if BLE drops mid-loop.

### Fixed
- **heritech_robot/src/main.cpp**: restore missing `sensors.begin()` and `state.setState(RobotState::IDLE)` in active `setup()` (they were only inside the commented-out legacy block) and remove the dead commented block that referenced `Serial.begin(115200)`. This fixes BLE connection failure: uninitialized I2C sensors (line tracer/color/gesture) were read every loop once a phone connected, stalling `BLE.poll()` so the phone's GATT discovery/connection-parameter update timed out and terminated the link (confirmed via nRF Connect: "Connection parameters update failed 0x16 → CONN TERMINATE"). After the fix the app reaches `[BLE] Ready` and nRF Connect discovers the Nordic UART Service.

### Changed
- **PLAN-BLE-FIX.md**: mark Fix 1/2/4 code items as done, keep hardware/conditional items open (firmware reflash, Step-0 hardware tests, Fix 3 GATT runbook, manager.destroy last resort).
- **heritech_robot/platformio.ini**: `monitor_speed` 115200 → 9600 to match `Serial.begin(9600)` (was unreadable before).
- **heritech_robot/src/main.cpp**: add temporary connected-loop time debug log — `[LOOP] max loop time:` every 3s to catch any blocking that would stall `BLE.poll()` (GATT discovery). Also fix `platformio.ini` comment `main.cpp:120`→`:122`.
- **heritage-buddy-app/src/lib/bluetooth.ts**: BLE discovery hardening — ~500ms settle delay after `connectToDevice()` before first discovery; retry backoff 1500ms → 2500ms; wrap `services()`/`service.characteristics()` in try/catch (timeout no longer misreported as "Connection error"); stale `onDisconnected` callback now only resets state when `bleState.device === connectedDevice` (old device disconnecting late can no longer wipe a live new connection).
- **heritage-buddy-app/src/hooks/use-robot-connection.ts**: auto-retry/auto-reconnect — after a failed `connect()` or an unexpected disconnect (not a user-initiated one), automatically retry the connection after 3s, max 2 attempts; uses `connectRef` synced via `useEffect` to avoid circular `useCallback` deps and React-compiler refs-during-render lint error.

### Changed
- **plan-ver2.md**: drop AprilTag/Radar entirely — Line Tracer + Color Sensor is the official sole navigation architecture, no future-direction section (removes references to `PLAN-APRILTAG.md`, which was never committed); `WARN:person` (PIR) replaces `ALARM` as the ACK pause source; SOS = long-press ≥2s on the existing Miniature Switch + app button; explicit ACK semantics table (person pauses + 10s `WARN_ACK_TIMEOUT_MS` timeout → `STATUS:auto_resumed`; turn warnings announce-only at the junction, `WARN:node` dropped as redundant with `NODE_START`); add BLE-loss auto-stop safety (`motors.stop()` on disconnect); fix dashboard polling 2s vs <5s target contradiction; reorder stages — software & interaction first, movement second; feature freeze from 17/08; fix section numbering.
- **heritage-buddy-app/src/lib/bluetooth.ts**: BLE UART discovery retry with backoff (3 attempts) instead of a hard 3.5s delay; register `onDisconnected` only after discovery succeeds so a failed discover no longer kills a live connection.
- **heritage-buddy-app/src/store/robot.ts**: add `activeWarn`, `robotStatus`, `sosActive` + setters.
- **heritage-buddy-app/src/types/robot.ts**: add `WARN:<type>` / `STATUS:<state>` to robot→app commands and `ACK` / `SOS` / `RESUME` to app→robot commands; add `WarnType`, `RobotStatusType`.
- **heritage-buddy-app/src/hooks/use-robot-connection.ts**: parse `WARN:`/`STATUS:` messages so they no longer log as unknown commands.
- **heritage-buddy-app/server/index.js**: add `/* global __dirname */` header to fix ESLint `no-undef`.
- **package.json / package-lock.json**: add `@react-native/jest-preset` override + dev dependency for Jest compatibility.
- **.gitignore**: ignore `.vscode/` and `compile_commands.json` (local editor/build artifacts).

### Removed
- **heritech_robot/src/config.h + main.cpp**: remove TEST MODE (auto-simulated `WARN`/`NODE_START` cycle + `TEST_WARN:<type>`/`TEST_NODE` commands + `sendWarn`/`handleTestMode` helpers). Robot now relies solely on real autonomous line-following; node opening is triggered by the existing Color Sensor red detection (3 stable reads) → `NODE_START:<id>`, unchanged.
- **heritech_robot/examples/**: delete `move_20cm` test-mode example sketch.

### Docs
- **CHANGELOG_BLE_FIX.md**: keep detailed write-up of the BLE discovery fix (root cause + before/after).
- **PLAN-BLE-FIX.md**: new fix plan for the current BLE discovery timeout (robot connects but `UART service not found`) — 4 hypotheses (H1 blocking poll in old firmware / H2 discovery too early / H3 Android GATT cache / H4 monitor_speed mismatch), prioritized fixes per layer (firmware non-blocking poll + debug loop-time log, app 500ms settle delay + larger backoff + auto-retry, GATT cache runbook, `monitor_speed` 115200→9600), test matrix (10 connects, reconnect, reboot) and Definition of Done (≥9/10 success, discovery <3s). Rev 2 (2026-08-05, after audit): fix deadline 08/08→14/08 (GATE 1 per plan-ver2), correct `main.cpp:120`→`:122` and remove non-existent `delay(300)` in `handleEnd`; add Step 0 zero-code tests (monitor-speed firmware fingerprint — old FW 115200 vs new 9600 — + forget-device GATT cache check); note the `attempt x/3` log proves current retry code fails 3/3 ⇒ systematic, H2 nearly ruled out; expand Fix 2 with try/catch around `services()`/`characteristics()`, stale `onDisconnected` callback bug fix (check `bleState.device === connectedDevice`), hook-level auto-retry/auto-reconnect, and `manager.destroy()` last resort; mark reconnect test as dependent on Fix 2.
